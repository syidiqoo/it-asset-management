# Taste
- Communicates in Indonesian (Bahasa Indonesia) and expects replies/explanations in Indonesian. Confidence: 0.7
- Prefers minimal, uncluttered UI: dislikes stacking summary/stat cards ("semakin banyak desainnya semakin jelek") and asks to remove them, replacing them with a lighter integrated pattern (e.g. an inline filter bar merged with the table). Also dislikes decorative/hero areas cluttered with action buttons ("jelek sekali") — expects an informational banner/hero to stay purely informational (title/subtitle/image only), with extraneous props, imports and variables cleaned up when the elements are removed. Confidence: 0.8
- Wants clear separation of concerns between pages/features: each page has one job (e.g. "Manajemen User" only fills in people into existing positions, "Manajemen Department" only manages the structure that those positions come from). Confidence: 0.6
- Requires additions to be strictly additive: never remove or break existing working features when implementing something new (stated emphatically, e.g. "jangan pernah menghapus fitur lain yang sudah bagus"). Confidence: 0.9
- Likes to see features running live locally rather than only reading a summary/build result — asks for the app to be started on a local dev server (and the data seeded) so he can try it himself, and repeatedly asks to be shown the result live ("perlihatkan kepadaku di live", "coba tampilkan live preview", "coba run di lokal live dulu"). Treats a local run as the first verification step and expects it *before* proceeding to a cloud/Vercel deploy — wants the real production flow (build + production server) exercised locally first rather than trusting deploy logs alone. After a change or a bug fix he also expects the app to be restarted and the whole flow re-tested locally ("restart test di lokal") instead of being told the work is done based on the code change alone. Confidence: 0.8
- Prefers the least invasive fix that solves the problem in place (e.g. keep file uploads in `public/uploads` with validation, rather than relocating them and rearchitecting access) — favors change that is simple and localized over broader restructuring. Confidence: 0.5
- Prefers a dark-dominant visual theme: near-black (Ubuntu-black) canvas with a single warm accent color (orange, e.g. Ubuntu #E95420) used sparingly, with dark as the default appearance. Also wants an explicit light/dark theme toggle so the user can switch (state persisted across reloads, no flash on load) — a hard-coded dark-only look is not enough. Confidence: 0.65
- Likes styling to be anchored to a concrete, well-known reference design and asks for it to be followed closely — e.g. explicitly "follow ubuntu.com style", naming primary/secondary button colors, hover states, and layout/heading scale as things to match. Confidence: 0.65
- Wants button components to expose distinct, explicitly defined variants with clear hover states (separate primary / secondary / outline / ghost styling, using dedicated hover color tokens rather than just an opacity tweak on the base color). Confidence: 0.5
- Design taste leans enterprise-SaaS/minimalist: clean cards, thin subtle borders, medium corner radius, minimal shadows, high contrast, generous whitespace, bold headings; explicitly dislikes excessive gradients, glassmorphism, neon colors, heavy drop shadows, over-rounded UI, and crowded layouts. Confidence: 0.65
- When he hands over detailed written design notes/specs, he expects them to be read carefully and followed precisely ("baca notes ini dengan teliti") instead of being loosely interpreted. Confidence: 0.55
- For new data-entry modules, expects existing master data to be reused instead of duplicated: the assigned person is picked from the existing User data ("seperti biasa usernya diambil dari data user") and dependent fields like Department derive from that user, not entered twice. Confidence: 0.65
- Enumerable attribute values should be modeled as managed master data with their own dedicated CRUD menu/page (e.g. a "Package" list with add/edit/delete) rather than free-text fields, and seeded with the example values he names. Confidence: 0.6
- Specifies table columns exhaustively and literally ("untuk tabelnya hanya ada ...") — expects the table to show exactly the listed columns, with no extra columns added. Confidence: 0.55
- Prefers sidebar navigation organized by importance: only primary workflows at top level (Dashboard, Data Aset, SIM Card), with secondary/master-data sections (User, Department, Kategori Inventaris, Package SIM) collapsed under a single dropdown parent ("Pengaturan"). Confidence: 0.5
- Prefers simple inline expand/collapse menus over popup/portal-based menus ("dropdown sederhana saja bukan popup") — sub-menus should open in place with a rotating chevron, not as a floating overlay. Confidence: 0.6
- Expects persistent chrome in the layout: sidebar navigation and its footer actions (user info, logout button) must stay pinned in view while the main content scrolls — the sidebar should be sticky/viewport-height with its own internal scroll rather than lengthening with the page. Confidence: 0.5
- Wants page-level controls on data-listing screens to be pinned too: the page header/title, action buttons (Export/Import/Add) and the filter/search bar should stay fixed at the top and NOT scroll away with the table ("header data aset, tombol, dan bagian filter fixed, tidak boleh ikut terscroll") — a desktop-app-like fixed toolbar with the content scrolling cleanly underneath it. Confidence: 0.6
- Wants to review and approve the implementation plan before changes touch the database/schema; he reads the plan and corrects assumptions (e.g. that CLS Domestic/Roaming are numbers), so plan-first is expected for migrations. Confidence: 0.5
- Cares that new UI features stay lightweight and don't burden the app ("yang penting bagus dan tidak membebankan aplikasi") — expects cheap rendering (pure CSS over JS, avoid extra DB queries, no heavy assets) rather than a rich/animated showpiece. Confidence: 0.6
- Leaves the aesthetic specifics of a new feature to the agent ("ukuranya menyesuaikan saja, yang penting bagus") as long as the result looks good and matches the existing design language — he specifies intent/constraints, not pixel details. Confidence: 0.55
- Context-dependent: previously wanted user-visible content admin-configurable from a settings page (dashboard banner edited via "Pengaturan → Banner"), but later reversed this for content he judged low-value, asking to remove the banner editor and make the banner fixed/hard-coded — the "make it configurable" expectation applies only to content actually worth managing, not as a default. Confidence: 0.4
- Prefers grouping static image assets into a dedicated subfolder (e.g. `public/img/`) referenced by a clean path instead of scattering them at the project root or `public/` root, and wants only a single canonical copy (old duplicates removed). Confidence: 0.6
- Prefers full-bleed background images rendered with `next/image` in `fill` mode (`object-cover`, dark gradient overlay so text stays legible) rather than a plain `<img>` or CSS background. Confidence: 0.5
- Prefers cutting a feature entirely when it seems low-value rather than building/maintaining configurability for it ("sepertinya tidak terlalu penting") — expects the unused page, form, action, validation schema, DB model/migration, sidebar entry and seed data to all be removed together. Confidence: 0.6
- Cares about keeping secrets out of version control: asks how to prevent `.env` from reaching GitHub, so expects `.gitignore` coverage for env files (with `.env.example` kept as a placeholder-only template) plus a way to verify nothing sensitive is tracked. Confidence: 0.4
- Cares about production readiness: before handing an app off to production, expects an end-to-end security/exploitability review that proactively hunts for gaps and bugs (auth bypass, upload exposure, session issues, rate limiting, headers). Confidence: 0.6
- Values durable written documentation kept in the repo as Markdown files: a simple user-facing guide (e.g. `PANDUAN.md`) that explains the existing features, and a separate technical document (e.g. `DOKUMENTASI.md`) describing the tech stack (with versions) and the folder/project structure. Confidence: 0.6
- Expects that technical documentation be kept current as the architecture changes, not written once: after a stack/deployment shift he asks for the doc to be updated with a new "peta konsep" (architecture concept map) section covering the new setup. Confidence: 0.5
- Expects documentation content to be derived from the actual codebase rather than guessed — the tech stack, versions and structure should be verified against real files (package.json, import statements, glob) so the doc is accurate. Confidence: 0.5
- Prefers to keep control over version control: the agent should leave changes uncommitted/unpushed and ask before committing or pushing, then act only once he explicitly authorizes it (e.g. replying "commit dan push ke git" after being told changes were uncommitted). Confidence: 0.55
- Deployment target has moved from cloud (originally Vercel, where build/deploy failures should be diagnosed against how Vercel actually builds — clean install, no locally generated artifacts) to self-hosting: he installs Docker on his own PC and wants the project run there via Docker Compose (with Postgres + uploads in volumes) instead of a managed PaaS. Confidence: 0.5
- When he has to set something up himself, he asks "apa saja yang harus aku lakukan" and expects a concrete, ordered, copy-pasteable runbook — exact commands, required env values, and how to verify each step — not just a high-level explanation of what Docker/Compose does. Confidence: 0.5
- Works on Windows (cmd.exe shell): commands and scripts handed to him must be Windows-compatible, and he expects Windows-friendly workarounds (temp-move/restore files, `dir /b`, `set`, `if exist`) when an operation has no native Unix equivalent. Confidence: 0.7
- Wants verification against the real deployed environment, not just local runs: after deploy prep he asks to "test di live" — expects the live URL to be exercised end-to-end (login, main pages, data showing correctly) and any errors reported, rather than treating local build/lint/seed results as sufficient proof. Since moving to self-hosting, "live" means the running Docker-deployed app (e.g. localhost:3000): after a code change he says "coba test live", expecting the container to be rebuilt (`docker compose up -d --build`) and the change re-verified interactively in the running deployment, not merely on a local dev server. He repeats this request per round ("coba test lagi di live") whenever he has new data to run through, so live testing in the running deployment is the standard loop rather than a one-time check. Confidence: 0.65
- Expects a project to be genuinely deployment-ready with zero remaining errors: when asking to prepare an app for deploy he wants an exhaustive end-to-end pass ("cek dari awal sampai akhir" — whole project: config, database, uploads, build and runtime), not a partial spot-check, and states plainly that he does not want any further errors ("aku tidak ingin ada error lagi"). Confidence: 0.6
- After an unplanned interruption (e.g. the PC dying mid-task), expects the agent to re-orient itself: inspect the last state of the work (git status/diff/log, in-progress files), confirm there are no errors, and then resume/continue the pending update — rather than starting over or asking for the full context again. Confidence: 0.5
- Grants the agent broad execution autonomy: when he says "lakukan saja semuanya" he expects it to actually carry the task through (edit config, run builds/docker commands, seed, verify) rather than hand back instructions, and to do "apa yang kamu bisa" (everything it can do itself) before clearly reporting the remaining manual steps he must perform. Confidence: 0.6
- Wants a clear, direct bottom-line verdict/answer when an outcome is uncertain (e.g. "jadi kesimpulannya tidak bisa di deploy di Vercel?") — expects the conclusion stated plainly and unambiguously (can/can't, and what's still needed), rather than a long hedged status report. Confidence: 0.4
- Prefers infinite scroll (auto-load on scrolling down) over "next/previous" pagination buttons for data listings, explicitly rejecting "selanjutnya" pages, and justifies it by anticipating future scale ("kedepanya data ini akan ada sampai ratusan") — show the first page of rows then lazily load more on scroll. Confidence: 0.6
- Prefers lenient, non-blocking data ingestion over strict all-or-nothing validation: when imported rows reference users, departments, or categories (invent types) that don't exist (or are ambiguous), he wants them labeled "Unknown"/"Anonim" and imported anyway rather than failing the whole batch, and expects the placeholder "Unknown" record for every fallback entity to be created up front (find-or-create, plus in the seed) so the fallback always works. The fallback must be exhaustive, not partial — when an import batch still failed after a first version he complained it "masih menampilkan import dibatalkan" until category references were covered too. Confidence: 0.7
- Expects UI to omit information that serves no purpose for the role/entity in question instead of displaying it anyway: he asked whether the Username field could be dropped for non-user accounts "karena mereka tidak memiliki akses" — the criterion for showing a field is real usefulness, so unused fields should be hidden from forms, tables and exports (not left blank/irrelevant). Confidence: 0.45
- Wants fallback/dirty data to be visible and easy to clean up later: after a lenient import he expects a notification telling him how many rows were labeled Unknown, plus a way to find them (e.g. filtering Data Aset by Department: Unknown) to correct them — and to be correctable in bulk rather than one row at a time. Confidence: 0.55
- When a real/legacy data file doesn't match the importer's expected schema, prefers normalizing the source file (rename headers to recognized names, normalize enum values like Condition to the exact casing, fill missing required columns with sensible defaults, drop ignored columns) and keeping the corrected copy next to the original (e.g. `real-case-fixed.csv`) over extending the importer with legacy header aliases or loosening its matching — he picked "Perbaiki file CSV-nya" rather than "Perluas importer-nya". This concerns file format/column mapping only and is consistent with his separate preference for lenient handling of missing reference data (user/department/category → Unknown). Confidence: 0.5
- Before importing into production he hands over a real-case sample (e.g. a folder like `contoh-import/` containing the actual source data) and expects the agent to inspect it precisely against the app's own parser/rules — reporting exactly what will pass, fail, or land as Unknown, and the concrete minimal changes required — before any data is imported. Confidence: 0.5
- Prefers preserving all data rows over discarding problematic ones: for asset codes used more than once he chose to auto-append a distinguishing suffix (e.g. `023-B/VI/2022-2`, `-3`) so every row can be imported, rather than dropping the duplicate rows. The same applies to a missing required field (empty `Code`): he picks auto-generating placeholder codes (e.g. `AUTO-PRINTER-001..006`) over dropping or blocking those rows, accepting they can be tidied up later via Edit Aset, and rates "rapikan otomatis" above "aku perbaiki sendiri dulu". Confidence: 0.55
- Wants the production database clean of leftover test/dummy data before real data is loaded — he approved deleting the earlier `DUM-*` dummy assets before importing the real-case dataset. Confidence: 0.45
- Treats the live DB as his real production store: his real dataset should be imported and left in place (not a test-then-rollback), and he accepts rows landing as Unknown/fallback for entities not yet in the master data. Confidence: 0.4
- Edits the import CSVs himself (in Excel) between rounds — the same file grows, e.g. 130 → 256 rows — so he expects the agent to re-open, re-validate and repair the current file (re-saved/broken header like `Kategori Inventaris;;;;;;;;;;;`, changed delimiter, extra empty columns) before each import instead of trusting the previously validated copy. He also reuses the same filename for entirely different data (e.g. `real-case-new-only.csv` went from 126 Phone rows to 108 PC/Printer rows) and just says "aku punya data baru lagi, coba cek file X", expecting the agent to re-inspect from scratch and re-diff against the *current* live DB state (asset codes already imported in earlier rounds must be re-checked each round, since the DB itself changed between rounds). Confidence: 0.55
- Prefers CSV as the exchange format for bulk data operations and expects a full round-trip workflow: export → edit in Excel → re-import to apply the changes (he asked for "fitur export import untuk user dan department, dalam bentuk csv saja"). Any master-data entity he works with should have both an export and an import path. Confidence: 0.5
- Wants bulk import to be able to *update* existing records (upsert), matched by an identifier such as ID (master data) or Code (assets), instead of only creating new rows — approved adding an "update aset yang sudah ada (berdasarkan Code)" option so existing assets can be repointed to real users/departments without deleting and re-importing them. Confidence: 0.5
- Requires careful handling of credentials in data exchange: password is mandatory for login-capable roles on newly imported users (no weak default), and password fields/hashes are import-only and never appear in exports. Confidence: 0.45
